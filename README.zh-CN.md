# 🚀 AeroSwap

<div align="center">

![Version](https://img.shields.io/badge/version-1.0.0-blue.svg)
![License](https://img.shields.io/badge/license-MIT-green.svg)
![Vue](https://img.shields.io/badge/Vue-3.4+-4FC08D?logo=vue.js)
![Vite](https://img.shields.io/badge/Vite-5.2+-646CFF?logo=vite)

**基于 Dolphinet 构建的现代化去中心化交易所 (DEX)**

[功能特性](#-功能特性) • [快速开始](#-快速开始) • [文档](#-文档) • [贡献指南](#-贡献指南)

</div>

---

## 📖 项目简介

AeroSwap 是一个基于 Dolphinet 网络构建的去中心化交易所协议，为用户提供流畅的代币兑换体验。平台具有直观的界面、多语言支持和强大的 DeFi 交易安全措施。

### ✨ 核心特性

- 🔄 **代币兑换**: 在 Dolphinet 上直接兑换代币，滑点最小化
- 💼 **钱包集成**: 通过 Wagmi 连接主流钱包
- 🌍 **多语言支持**: 完整支持中文和英文
- ⚙️ **可自定义滑点**: 可调整的滑点容差设置
- 🎨 **现代化 UI**: 简洁、响应式设计，流畅的动画效果
- 🔒 **安全交易**: 采用安全最佳实践构建
- 📱 **响应式设计**: 针对桌面和移动设备优化
- 🌐 **网络支持**: 兼容 Dolphinet 主网和测试网

## 🛠️ 技术栈

### 核心技术
- **Vue 3** - 渐进式 JavaScript 框架
- **Vite** - 下一代前端构建工具
- **TypeScript** - 类型安全的 JavaScript
- **Pinia** - 状态管理
- **Vue Router** - 客户端路由

### 区块链集成
- **Wagmi** - Ethereum React Hooks (Vue 移植版)
- **Ethers.js** - Ethereum JavaScript 库
- **Viem** - TypeScript Ethereum 库
- **Uniswap V2 SDK** - DEX 协议集成

### UI 和样式
- **Element Plus** - Vue 3 组件库
- **SCSS** - CSS 预处理器
- **Animate.css** - 动画库

### 国际化
- **Vue I18n** - 国际化插件

## 📦 安装

### 前置要求

- **Node.js** >= 18.0.0
- **npm** >= 9.0.0 或 **pnpm** >= 8.0.0

### 安装步骤

1. **克隆仓库**
   ```bash
   git clone <repository-url>
   cd swap-app
   ```

2. **安装依赖**
   ```bash
   npm install
   # 或
   pnpm install
   ```

3. **配置环境变量**
   
   在项目根目录创建 `.env` 文件：
   ```env
   VITE_CHAIN_ID=your_chain_id
   VITE_RPC_URL=your_rpc_url
   ```

## 🚀 快速开始

### 开发模式

启动开发服务器：

```bash
npm run dev
```

应用将在 `http://localhost:3000` 可用

### 构建

构建生产版本：

```bash
npm run build
```

构建文件将位于 `dist` 目录

### 预览

预览生产构建：

```bash
npm run preview
```

### 代码检查

运行代码检查：

```bash
npm run lint
```

## 📁 项目结构

```
swap-app/
├── src/
│   ├── assets/          # 静态资源（图片、字体等）
│   ├── components/      # Vue 组件
│   │   ├── navBar.vue   # 导航栏组件
│   │   └── popup.vue    # 弹窗组件
│   ├── config/          # 配置文件
│   ├── languages/       # 国际化文件
│   │   └── locales/
│   │       ├── en_US.js # 英文翻译
│   │       └── zh-CN.js # 中文翻译
│   ├── router/          # Vue Router 配置
│   ├── stores/          # Pinia 状态管理
│   ├── styles/          # 全局样式
│   │   ├── fonts.scss   # 字体定义
│   │   ├── global.scss  # 全局样式
│   │   └── themes.scss  # 主题变量
│   ├── utils/           # 工具函数
│   ├── views/           # 页面组件
│   │   └── swap/
│   │       ├── index.vue      # 主兑换页面
│   │       ├── doSwap.js       # 兑换执行逻辑
│   │       ├── uniswapQuote.js # 报价估算
│   │       ├── tokenSelect.vue # 代币选择弹窗
│   │       └── SlippageModal.vue # 滑点设置
│   ├── App.vue          # 根组件
│   ├── main.ts          # 应用入口
│   └── wagmi.ts         # Wagmi 配置
├── public/              # 公共资源
├── dist/                # 构建输出
├── index.html           # HTML 模板
├── vite.config.ts       # Vite 配置
├── tsconfig.json        # TypeScript 配置
└── package.json         # 项目依赖
```

## 🎯 功能说明

### 代币兑换

- 在原生 DOL 代币和 Dolphinet 代币之间兑换
- 实时价格报价
- 最小接收金额计算
- 交易状态跟踪

### 钱包连接

- 支持多种钱包提供商
- 账户余额显示
- 交易历史
- 网络切换

### 用户界面

- **浅色主题**: 现代、简洁的设计
- **响应式布局**: 适配所有屏幕尺寸
- **流畅动画**: 精致的背景动画效果
- **无障碍支持**: 键盘导航支持

### 国际化

- **英文**: 完整的英文语言支持
- **中文（简体）**: 完整的中文翻译
- **语言切换**: 导航栏中轻松切换语言

## 🔧 配置

### 网络配置

在 `src/config/` 中配置支持的网络：

```typescript
export const networks = {
  mainnet: {
    chainId: 1,
    rpcUrl: 'https://mainnet.dolphinet.com',
    // ...
  },
  testnet: {
    chainId: 2,
    rpcUrl: 'https://testnet.dolphinet.com',
    // ...
  }
}
```

### 主题定制

在 `src/styles/themes.scss` 中修改主题变量：

```scss
:root {
  --bg-color: #ffffff;
  --text-color: #1a1a1a;
  --el-menu-active-color: #00CE7A;
  // ...
}
```

## 📚 文档

### 开发指南

1. **添加新代币**
   - 在代币列表中添加代币信息
   - 配置代币元数据（符号、小数位数、地址）

2. **自定义样式**
   - 在 `themes.scss` 中修改 SCSS 变量
   - 在相应的 `.vue` 文件中更新组件样式

3. **添加翻译**
   - 在 `src/languages/locales/en_US.js` 中添加新键
   - 在 `zh-CN.js` 中添加对应的翻译

### API 参考

- **兑换函数**: `doSwaps()` 位于 `src/views/swap/doSwap.js`
- **报价估算**: `estimateQuotes()` 位于 `src/views/swap/uniswapQuote.js`

## 🚢 部署

### Docker 部署

使用 Docker 构建和运行：

```bash
docker build -t aeroswap .
docker run -p 3000:3000 aeroswap
```

### 静态托管

`dist` 文件夹包含可部署到以下平台的静态文件：

- **Vercel**: `vercel deploy`
- **Netlify**: 拖放 `dist` 文件夹
- **GitHub Pages**: 在仓库设置中配置
- **Nginx**: 将 `dist` 内容复制到 Web 根目录

### 环境变量

为生产环境设置以下环境变量：

```env
VITE_CHAIN_ID=production_chain_id
VITE_RPC_URL=production_rpc_url
VITE_ROUTER_ADDRESS=router_contract_address
```

## 🤝 贡献指南

欢迎贡献！请遵循以下步骤：

1. Fork 本仓库
2. 创建功能分支 (`git checkout -b feature/amazing-feature`)
3. 提交更改 (`git commit -m '添加某个功能'`)
4. 推送到分支 (`git push origin feature/amazing-feature`)
5. 开启 Pull Request

### 代码风格

- 遵循 Vue 3 Composition API 最佳实践
- 使用 TypeScript 确保类型安全
- 编写有意义的提交信息
- 为复杂逻辑添加注释

## 🐛 已知问题

- 在低端设备上可能会禁用某些动画
- 大型代币列表可能导致性能问题（考虑分页）

## 📝 许可证

本项目采用 MIT 许可证 - 查看 [LICENSE](LICENSE) 文件了解详情。

## 🙏 致谢

- [Uniswap](https://uniswap.org/) 提供 DEX 协议
- [Wagmi](https://wagmi.sh/) 提供钱包集成
- [Element Plus](https://element-plus.org/) 提供 UI 组件
- [Vue.js](https://vuejs.org/) 社区

## 📞 支持

如需支持，请在 GitHub 仓库中提交 issue 或联系开发团队。

---

<div align="center">

**为 Dolphinet 社区用心打造 ❤️**

[报告 Bug](https://github.com/your-repo/issues) • [功能请求](https://github.com/your-repo/issues) • [文档](https://docs.aeroswap.com)

</div>

